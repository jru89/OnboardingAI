---
work_package_id: WP04
title: 'Modules 4 & 6: Reviewing Changes and Prompt Fallibility'
dependencies: []
requirement_refs:
- FR-005
- FR-006
tracker_refs: []
planning_base_branch: feat/safety-judgment-additions
merge_target_branch: feat/safety-judgment-additions
branch_strategy: Planning artifacts for this mission were generated on feat/safety-judgment-additions. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/safety-judgment-additions unless the human explicitly redirects the landing branch.
subtasks:
- T011
- T012
- T013
phase: Phase 2 - Module Content
assignee: ''
agent: ''
history:
- at: '2026-09-04T11:10:00Z'
  actor: system
  action: Prompt generated via /spec-kitty.tasks
agent_profile: frontend-freddy
authoritative_surface: js/data/modules/
create_intent: []
execution_mode: code_change
model: ''
owned_files:
- js/data/modules/04-repos.js
- js/data/modules/06-prompting-101.js
role: implementer
tags: []
task_type: implement
---

# Work Package Prompt: WP04 – Modules 4 & 6: Reviewing Changes and Prompt Fallibility

## ⚡ Do This First: Load Agent Profile

Use the `/ad-hoc-profile-load` skill to load the agent profile specified in the frontmatter, and behave according to its guidance before parsing the rest of this prompt.

- **Profile**: `frontend-freddy` | **Role**: `implementer` | **Agent/tool**: `claude`

## ⚠️ IMPORTANT: Review Feedback

Check the `review_ref` field in the event log before starting. Address all feedback before your work is complete.

## Markdown Formatting

Wrap HTML/XML tags in backticks. Use language identifiers in code blocks.

---

## Objectives & Success Criteria

Two small, independent content-only additions to two different modules,
grouped into one WP because each is too small to justify its own lane.
Neither touches any lab config.

- **Module 4 (Repos)**: a new section giving a light before/after mental
  model for reviewing an AI assistant's changes to a project (FR-005).
- **Module 6 (Prompting 101)**: a new section establishing that a good
  prompt doesn't guarantee a correct answer, explicitly cross-referencing
  Module 8's existing "treat the first reply as a draft" material
  (FR-006).

## Context & Constraints

- No dependencies -- this WP can start immediately in parallel with any
  other WP in this mission.
- Read [`spec.md`](../spec.md) FR-005, FR-006, and Acceptance Scenarios 3
  and 4.
- Read [`data-model.md`](../data-model.md)'s table row for each module (new
  section heading, glossary-term note for Module 4).
- **Module 4** ([`js/data/modules/04-repos.js`](../../../js/data/modules/04-repos.js)):
  read the whole file first. It already has a folder-tree diagram and a
  "What's usually inside" section listing common files. FR-005 explicitly
  says this is **not a Git course** -- avoid introducing "diff," "commit
  history," or similar terms as if teaching Git; describe before/after
  entirely in plain language ("this is what the project looked like...
  this is what changed"). `data-model.md` notes the term "diff" is
  deliberately *not* used here even though it's defined elsewhere
  (Module 11) -- do not add a `glossaryTerms` entry for it in this
  section.
- **Module 6** ([`js/data/modules/06-prompting-101.js`](../../../js/data/modules/06-prompting-101.js)):
  read the whole file first, especially its existing "The golden rules"
  section and its "A worked example" section (weak vs. strong prompt).
  The new section should read as a natural companion to "The golden
  rules" -- immediately after it or after the worked example, whichever
  placement reads better once you've read the whole module (your
  judgment call; either is acceptable, note which you chose and why in
  this WP's Activity Log entry).
- Module 8's existing "Treat the first reply as a draft" section (in
  [`js/data/modules/08-prompting-201.js`](../../../js/data/modules/08-prompting-201.js),
  read-only reference -- **do not edit this file**, it belongs to WP05)
  is what Module 6's new section should explicitly point forward to, e.g.
  "you'll practice checking AI output more in Module 8" or similar --
  read that section's exact wording so your cross-reference is accurate,
  not a guess.
- Both additions are content-only -- no `labs` array changes in either
  file.

## Branch Strategy

- **Strategy**: already-confirmed
- **Planning base branch**: feat/safety-judgment-additions
- **Merge target branch**: feat/safety-judgment-additions

## Subtasks & Detailed Guidance

### Subtask T011 – Author Module 4's "How do I know what changed?" section

- **Purpose**: A before/after mental model for reviewing an AI assistant's
  changes.
- **Steps**:
  1. Add a new content-section entry to `04-repos.js`'s `content` array
     (append after the existing sections), heading along the lines of
     "How do I know what changed?" (exact wording your call, matching
     `data-model.md`'s intent).
  2. Body: introduce the mental model plainly -- "Before: this is what
     the project looked like. After: Claude changed these things.
     Review: do those changes make sense?" -- then a small, concrete
     before/after example. Keep the example small enough to read at a
     glance (matching the module's existing folder-tree diagram's scale,
     e.g. a short plain-text or `<pre>` before/after file listing showing
     one file changed and one file added -- not a sprawling tree).
  3. Embed 1-2 reflection questions directly in the prose (not a graded
     interaction) -- e.g. "which things changed?" and "which change would
     you want to look at more closely before accepting it?" -- as
     rhetorical/self-directed questions the reader considers while
     reading, not as form inputs.
  4. Explicitly avoid Git-course territory: no "diff," "commit," "commit
     history," or similar terms as taught concepts here -- plain
     "before/after" language only, per FR-005.
- **Files**: `js/data/modules/04-repos.js`
- **Parallel?**: [P] -- independent of T012 (different file).

### Subtask T012 – Author Module 6's "A good prompt doesn't guarantee a good answer" section

- **Purpose**: The complementary lesson to "getting a good answer" --
  getting a good answer isn't guaranteed even with a good prompt.
- **Steps**:
  1. Add a new content-section entry to `06-prompting-101.js`'s `content`
     array, heading "A good prompt doesn't guarantee a good answer,"
     placed either right after "The golden rules" or right after "A
     worked example" -- read both existing sections first and pick
     whichever placement flows better; note your choice in this WP's
     Activity Log.
  2. Body: cover that Claude Code can misunderstand, make assumptions, or
     invent information; that a detailed-looking answer isn't necessarily
     a correct one; that the learner is responsible for checking
     important results.
  3. Introduce the rule "AI output is a draft until you've checked it" as
     a clear, quotable line (matching this module's existing pattern of
     calling out memorable phrases, e.g. its worked example's "that's
     iteration in practice" closing line).
  4. Explicitly cross-reference Module 8's existing "treat the first
     reply as a draft" material by name (e.g. "you'll practice this
     directly in Module 8") -- read that section in
     `08-prompting-201.js` first so the reference is accurate to its
     actual wording, not a paraphrase from memory.
- **Files**: `js/data/modules/06-prompting-101.js`
- **Parallel?**: [P] -- independent of T011 (different file).

### Subtask T013 – Manual browser verification of both new sections

- **Purpose**: Confirm both additions render correctly, including at
  narrow viewport width.
- **Steps**:
  1. Start the local preview server, open Module 4, confirm the new
     section renders in the right place with correct styling (the
     before/after example, if using a `<pre>` block, should wrap
     correctly at 360px per this app's existing `pre` styling -- do not
     let it overflow).
  2. Open Module 6, confirm the new section renders in the right place
     and the cross-reference to Module 8 reads sensibly.
  3. Resize to 360px width and re-check both sections for layout
     breakage.
  4. Check the browser console for errors on both modules.
- **Files**: none changed -- verification only.
- **Parallel?**: No -- final step, depends on T011 and T012.

## Risks & Mitigations

- **Risk**: Module 4's before/after example accidentally teaches Git
  concepts despite FR-005's explicit "not a Git course" constraint.
  **Mitigation**: after writing, re-read the section and confirm it would
  make sense to someone who has never heard of Git, diffs, or commits --
  if it wouldn't, simplify further.
- **Risk**: Module 6's cross-reference to Module 8 goes stale if Module
  8's section wording changes later. **Mitigation**: reference the *idea*
  ("treat the first reply as a draft," Module 8) rather than quoting
  Module 8's exact sentence verbatim, so the cross-reference stays valid
  even if WP05's Module 8 edits (a different, parallel work package)
  adjust nearby wording.

## Review Guidance

- Confirm Module 4's new section contains no Git-course terminology
  ("diff," "commit," "history") as taught concepts.
- Confirm Module 6's new section explicitly names Module 8 and the
  "treat the first reply as a draft" idea.
- Load both modules in the browser at desktop and 360px width and confirm
  no layout breakage.

## Activity Log

**Initial entry**:

- 2026-09-04T11:10:00Z – system – Prompt created.

### Updating Status

Status is managed via `status.events.jsonl`. Use `spec-kitty agent tasks move-task WP04 --to <status>` to change WP status.
